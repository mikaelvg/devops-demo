spring:
  application:
    name: config-server1        g g AhHgz                   GC  a
  cloud:
    config:
      server:
        git:
            uri: https://github.com/mikaelvg/devops-demo
            searchPaths: 'config-repo/{application}/{profile}'
server:
  port: 7100x